import styled from '@emotion/styled';

const CheckboxContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 4px;
  user-select: none;
  cursor: pointer;
`;

export { CheckboxContainer };
